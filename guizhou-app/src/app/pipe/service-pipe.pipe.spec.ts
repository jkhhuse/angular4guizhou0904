import { ServicePipePipe } from './service-pipe.pipe';

describe('ServicePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ServicePipePipe();
    expect(pipe).toBeTruthy();
  });
});
