import { RegNameDirective } from './reg-name.directive';

describe('RegNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RegNameDirective();
    expect(directive).toBeTruthy();
  });
});
